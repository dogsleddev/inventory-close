# Prompt Packages

- `design/README.md` — staged Claude Design sequence. Run this first.
- `code/README.md` — staged Claude Code implementation sequence. Run only after the final approved design handoff is saved under `/design`.

Canonical references live at repository root and in `/docs`. Both prompt packages must preserve the locked FY2026 financial baseline and NetSuite read-only boundary.
