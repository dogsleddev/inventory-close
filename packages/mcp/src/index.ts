/**
 * @icg/mcp — future TypeScript MCP adapter (P2, docs/05).
 * Placeholder only: it will expose application services as MCP tools with the
 * same permission inheritance as Ask Gaurd.
 */

export const MCP_ADAPTER_STATUS = "NOT_IMPLEMENTED_P2" as const;
